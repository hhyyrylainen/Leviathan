//GUIBasePage = "Pong.html";
GUIBasePage = "http://www.google.fi/";
GUISize = FULL;

objects {
    // this keeps the GUI always on //
    o GuiCollection "OnKeeper"{
        l params{
            Enabled = true;
            KeepsGUIOn = true;
        }
    }
}
